<h1>New feed</h1>

<% form_for(@feed) do |f| %>
  <fieldset>
  <%= f.error_messages %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', feeds_path %>