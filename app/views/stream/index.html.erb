<h1>Lifestream - <%= link_to 'Refresh', :controller => 'feeds', :action => 'refresh' %></h1>

<ol>
<%- @items.each do |item| -%>
  <% item.pretty %>
  <li class="<%= item.type %>">
    <h2><%= item.title %></h2>
    <a href="<%= item.permalink %>"><%= item.date %></a>
  </li>
<%- end -%>
</ol>